#include <map>
#include <iostream>
#include <unordered_map>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	unordered_map<string, 

	int n, m;
	cin >> n, m;

	for (int i = 0; i < n; i++)
	{
		cin >> name;
		map1[name] = 1;
	}
	for (int i = 0; i < m i++)
	{

		cin >> name;
		if (map1[name]) map2[name];
	}
	

}